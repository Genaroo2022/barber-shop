# Copy this file to ".env" (or export vars in shell) before running docker compose.

POSTGRES_PASSWORD=CHANGE_ME_DB_PASSWORD
DB_PASSWORD=CHANGE_ME_DB_PASSWORD
JWT_SECRET_BASE64=REPLACE_WITH_BASE64_SECRET

BOOTSTRAP_ADMIN_ENABLED=true
BOOTSTRAP_ADMIN_EMAIL=admin@barberia.local
BOOTSTRAP_ADMIN_PASSWORD=CHANGE_ME_ADMIN_PASSWORD
